# Development: leave empty (uses localhost:3001 via rewrite)
# Production: Supabase REST API
# All client requests go through Next.js rewrites (/api/* -> API_URL/*),
# so the Supabase URL is never exposed to the browser.
API_URL=https://your-project.supabase.co/rest/v1
