import { Sora, Onest } from 'next/font/google'

export const sora = Sora({
  subsets: ['latin'],
  variable: '--font-sora',
  display: 'swap',
})

export const onest = Onest({
  subsets: ['latin'],
  variable: '--font-onest',
  display: 'swap',
})
