name: Deploy
on:
  push:
    branches: [dev, main, master]

jobs:
  schema:
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name == 'main' || github.ref_name == 'master' && 'production' || 'development' }}
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - run: supabase link --project-ref $SUPABASE_PROJECT_ID
      - run: supabase db push

  deploy:
    runs-on: ubuntu-latest
    needs: schema
    environment: ${{ github.ref_name == 'main' || github.ref_name == 'master' && 'production' || 'development' }}
    env:
      API_URL: ${{ secrets.SUPABASE_URL }}/rest/v1
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run build

      # Vercel deploy (primary for Next.js)
      - name: Deploy to Vercel
        if: ${{ secrets.VERCEL_TOKEN != '' }}
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: ${{ github.ref_name == 'main' || github.ref_name == 'master' && '--prod' || '' }}
          working-directory: frontend
